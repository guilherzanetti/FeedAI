// src/utils/downloadHelper.js
// A biblioteca html2canvas precisa estar disponível globalmente ou ser importada
export function saveDivAsPng(divId, filename = 'div-image.png') {
  // Sua função saveDivAsPng original vai aqui
}